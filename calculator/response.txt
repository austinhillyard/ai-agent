The calculator renders results to the console by evaluating the input expression, then formatting both the expression and the result into a box-like string using the `render` function found in the `pkg/render.py` module. Finally, the formatted string is printed to the console using the `print` function.